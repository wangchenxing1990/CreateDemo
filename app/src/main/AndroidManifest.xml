<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mvp.wangyukui.com.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".defineRecycler.DefineRecyclerActivity" />
        <activity
            android:name=".WebNewActivity"
            android:theme="@android:style/Theme.Translucent" />
        <activity android:name=".statemode.StateActivity" />
        <activity android:name=".statemode.LoginActivity" />
        <activity android:name=".rxjava.RxJavaActivity" />
        <activity android:name=".mvp.MvpActivity" />
        <activity android:name=".duty.DutyActivity" />
        <activity android:name=".strategyMode.StrategyActivity" />
        <activity android:name=".defineItem.DefineItemActivity" />
        <activity android:name=".cycler.CyclerActivity" />

    </application>
</manifest>